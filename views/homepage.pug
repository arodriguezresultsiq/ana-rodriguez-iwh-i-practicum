html
  head
    title Homepage
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="/css/style.css")
  body
    .table-container
      table
        thead
          tr
            th Name
            th Type
            th Bio
        tbody
          each pet in pets
            tr
              td= pet.properties.name
              td= pet.properties.type
              td= pet.properties.bio

    a.back-link(href='/update-cobj') Add to this table